# Setup

```sh
Download 'git bash'
Open & configure git with github username, email:
    > git config --global user.name "your_username"
    > git config --global user.email "your_email"
Check your configuration
    > git config --list
```
